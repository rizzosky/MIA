# MIA
Repo del proyecto para el master en IA de Martin Rizzo

Instalar python o funciona tambien en colab.

Instalar las dependencias (pip install -r requirements.txt)

Se ajustan los hiperparametros y configuraciones de entrenamiento y representacion de los datos en config.py

En main.py hay que elegir que tipo de entrenamiento usar (supervisado o no supervisado)

Correr main.py